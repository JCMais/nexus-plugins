describe('elasticApmInstrumentation', () => {
  describe('basic behavior', () => {
    // @TODO Add mocked tests later on
    it('should have tests', async () => {
      expect(1).toBe(1)
    })
  })
})
